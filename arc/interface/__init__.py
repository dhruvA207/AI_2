"""User-facing interfaces.

The CLI is the interface for now; ``server.py`` (a local HTTP/WebSocket API for a
future GUI) arrives in Phase 7. Both are thin: they translate between a human and the
agent, and hold no logic of their own.
"""
